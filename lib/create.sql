CREATE TABLE bears (
    id INTEGER PRIMARY KEY,
    name TEXT,
    age INTEGER,
    gender TEXT,
    color TEXT,
    temperament TEXT,
    alive BOOLEAN
);

-- INSERT INTO bears (d, name, age, gender, color, temperament, alive) VALUES ()